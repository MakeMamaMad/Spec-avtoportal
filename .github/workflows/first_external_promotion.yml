name: Legacy — External Article Publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-by-default:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy publisher is manual only
        run: |
          echo "Article syndication is no longer part of the active promotion strategy."
          echo "Use the Site Advertising workflow for direct SpecAvtoPortal promotion."
